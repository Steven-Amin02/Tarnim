<Window x:Class="Tarnim.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tarnim"
        mc:Ignorable="d"
        Title="Tarnim Splash" 
        Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">

    <Window.Resources>
        <Storyboard x:Key="FadeInStoryboard">
            <DoubleAnimation Storyboard.TargetName="MainBorder" 
                             Storyboard.TargetProperty="Opacity" 
                             From="0" To="1" Duration="0:0:0.8">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ThicknessAnimation Storyboard.TargetName="MainBorder" 
                                Storyboard.TargetProperty="Margin" 
                                From="0,20,0,0" To="0" Duration="0:0:0.8">
                <ThicknessAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </ThicknessAnimation.EasingFunction>
            </ThicknessAnimation>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Border x:Name="MainBorder" 
                CornerRadius="25" 
                Opacity="0">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0f172a" Offset="0"/> <!-- Slate 900 -->
                    <GradientStop Color="#1e293b" Offset="1"/> <!-- Slate 800 -->
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="25" ShadowDepth="0" Color="Black" Opacity="0.5"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <!-- Logo -->
                <Image Source="/logo.png" Width="120" Height="120" Margin="0,0,0,20" RenderOptions.BitmapScalingMode="HighQuality"/>
                
                <!-- App Title -->
                <TextBlock Text="ترنيــمات روحيــة" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="#f8fafc" 
                           HorizontalAlignment="Center"
                           FontFamily="Segoe UI"/>

                <!-- Subtitle / Loading -->
                <TextBlock Text="جاري التحميل..." 
                           FontSize="14" 
                           Foreground="#94a3b8" 
                           HorizontalAlignment="Center" 
                           Margin="0,15,0,0"/>
                
                <!-- Simple Loading Bar -->
                <ProgressBar IsIndeterminate="True" 
                             Width="200" 
                             Height="4" 
                             Margin="0,20,0,0" 
                             BorderThickness="0"
                             Background="#334155"
                             Foreground="#fbbf24"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
