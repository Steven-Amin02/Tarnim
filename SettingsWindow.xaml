<Window x:Class="Tarnim.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="الإعدادات - Settings" 
        Height="580" Width="550"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0f172a"
        FlowDirection="RightToLeft"
        Foreground="#f8fafc"
        WindowStyle="None"
        AllowsTransparency="False">
    
    <Window.Resources>
        <!-- Accent Gradient Button -->
        <Style x:Key="GoldButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#0f172a"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="8">
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                    <GradientStop Color="#fbbf24" Offset="0"/>
                                    <GradientStop Color="#f59e0b" Offset="1"/>
                                </LinearGradientBrush>
                            </Border.Background>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <Style x:Key="SecondaryButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="#f8fafc"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="#334155" 
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#475569"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1e293b"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RadioButton Style for Theme Toggle -->
        <Style x:Key="ThemeRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,15,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border"
                                Background="#1e293b"
                                BorderBrush="#334155"
                                BorderThickness="2"
                                CornerRadius="8"
                                Padding="15,8"
                                Cursor="Hand">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#fbbf24"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#334155"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="#fbbf24"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- RadioButton Style for Color Picker -->
        <Style x:Key="ColorRadioButtonStyle" TargetType="RadioButton">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,0,12,12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="colorBorder"
                                Width="50"
                                Height="50"
                                CornerRadius="25"
                                BorderThickness="2"
                                BorderBrush="#64748b"
                                Cursor="Hand"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="colorBorder" Property="BorderBrush" Value="#94a3b8"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="colorBorder" Property="BorderBrush" Value="#fbbf24"/>
                                <Setter TargetName="colorBorder" Property="BorderThickness" Value="3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#0f172a">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar with Controls on Right -->
        <Grid x:Name="titleBar" Grid.Row="0" Height="32" Background="#0f172a" FlowDirection="LeftToRight">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Title Text (RTL) -->
            <TextBlock Grid.Column="0" 
                       Text="الإعدادات" 
                       Foreground="#f8fafc" 
                       FontSize="14"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Margin="15,0,0,0"
                       FlowDirection="RightToLeft"/>

            <!-- Window Controls on Right -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,5,0">
                <Button Content="−" Width="40" Height="32" Background="Transparent" Foreground="#f8fafc" 
                        FontSize="18" Click="MinimizeButton_Click" BorderThickness="0" Cursor="Hand"
                        ToolTip="Minimize"/>
                <Button Content="□" Width="40" Height="32" Background="Transparent" Foreground="#f8fafc" 
                        FontSize="16" Click="MaximizeButton_Click" BorderThickness="0" Cursor="Hand"
                        ToolTip="Maximize"/>
                <Button Width="40" Height="32" Background="Transparent" Foreground="#f8fafc" 
                        FontSize="16" Click="CloseButton_Click" BorderThickness="0" Cursor="Hand"
                        ToolTip="Close">
                    <ui:SymbolIcon Symbol="Dismiss24" Style="{StaticResource TopBarIconStyle}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Grid Margin="30,25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="25"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Title -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <ui:SymbolIcon Symbol="Settings24" Style="{StaticResource ActionIconStyle}" Margin="0,0,10,0"/>
                    <TextBlock Text="الإعدادات" 
                               FontSize="28" 
                               FontWeight="Bold" 
                               Foreground="#f8fafc"/>
                </StackPanel>
                
                <!-- Theme Toggle -->
                <StackPanel Grid.Row="2">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="Color24" Style="{StaticResource ActionIconStyle}" Margin="0,0,8,0"/>
                        <TextBlock Text="اختر المظهر" 
                                   Foreground="#f8fafc" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="ThemeDark" 
                                     GroupName="Theme" 
                                     IsChecked="True"
                                     Checked="ThemeRadio_Checked"
                                     Style="{StaticResource ThemeRadioButtonStyle}"
                                     Margin="0,0,10,0">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="WeatherMoon24" Style="{StaticResource TopBarIconStyle}"/>
                                <TextBlock Text="داكن" Foreground="#f8fafc" FontWeight="SemiBold" FontSize="14" Margin="8,0,0,0"/>
                            </StackPanel>
                        </RadioButton>
                        <RadioButton x:Name="ThemeLight" 
                                     GroupName="Theme"
                                     Checked="ThemeRadio_Checked"
                                     Style="{StaticResource ThemeRadioButtonStyle}">
                            <StackPanel Orientation="Horizontal">
                                <ui:SymbolIcon Symbol="WeatherSunny24" Style="{StaticResource TopBarIconStyle}"/>
                                <TextBlock Text="فاتح" Foreground="#f8fafc" FontWeight="SemiBold" FontSize="14" Margin="8,0,0,0"/>
                            </StackPanel>
                        </RadioButton>
                    </StackPanel>
                </StackPanel>
                
                <!-- Font Size Setting -->
                <StackPanel Grid.Row="4">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="TextFont24" Style="{StaticResource ActionIconStyle}" Margin="0,0,8,0"/>
                        <TextBlock Text="حجم خط الكلمات" 
                                   Foreground="#f8fafc" 
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="70"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="FontSizeSlider" 
                                Grid.Column="0"
                                Minimum="14" 
                                Maximum="48" 
                                Value="28"
                                TickFrequency="2"
                                IsSnapToTickEnabled="True"
                                ValueChanged="FontSizeSlider_ValueChanged"
                                Foreground="#fbbf24"
                                Background="#1e293b"
                                VerticalAlignment="Center"
                                Margin="0,0,15,0"
                                Height="6"/>
                        <TextBlock x:Name="FontSizeText" 
                                   Grid.Column="1"
                                   Text="28" 
                                   Foreground="#fbbf24" 
                                   FontSize="18" 
                                   FontWeight="Bold"
                                   HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Background="#1e293b"
                                   Padding="8,4"
                                   TextAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="FontPreview" 
                               Text="مثال على حجم الخط" 
                               Foreground="#94a3b8" 
                               FontSize="28"
                               Margin="0,15,0,0"
                               TextAlignment="Center"
                               Background="#1e293b"
                               Padding="15,10"/>
                </StackPanel>
                
                <!-- Background Color Setting -->
                <StackPanel Grid.Row="6">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <ui:SymbolIcon Symbol="PaintBrush24" Style="{StaticResource ActionIconStyle}" Margin="0,0,8,0"/>
                        <TextBlock Text="اختر لون الخلفية" 
                                   Foreground="#f8fafc" 
                                   FontSize="16"
                                   FontWeight="SemiBold"/>
                    </StackPanel>
                    <WrapPanel>
                        <!-- Royal Dark (Default) -->
                        <RadioButton x:Name="ColorRoyalDark" 
                                     GroupName="BgColor" 
                                     IsChecked="True"
                                     Checked="ColorRadio_Checked"
                                     Tag="#0f172a"
                                     ToolTip="داكن ملكي"
                                     Margin="0,0,12,12">
                            <Border Width="50" Height="50" CornerRadius="25" Background="#0f172a" BorderBrush="#64748b" BorderThickness="2"/>
                        </RadioButton>
                        
                        <!-- Dark Slate -->
                        <RadioButton x:Name="ColorDarkSlate" 
                                     GroupName="BgColor"
                                     Checked="ColorRadio_Checked"
                                     Tag="#1e293b"
                                     ToolTip="الرمادي الداكن"
                                     Margin="0,0,12,12">
                            <Border Width="50" Height="50" CornerRadius="25" Background="#1e293b" BorderBrush="#64748b" BorderThickness="2"/>
                        </RadioButton>
                        
                        <!-- Deep Navy -->
                        <RadioButton x:Name="ColorDeepNavy" 
                                     GroupName="BgColor"
                                     Checked="ColorRadio_Checked"
                                     Tag="#172554"
                                     ToolTip="الأزرق العميق"
                                     Margin="0,0,12,12">
                            <Border Width="50" Height="50" CornerRadius="25" Background="#172554" BorderBrush="#64748b" BorderThickness="2"/>
                        </RadioButton>
                        
                        <!-- Pure Black -->
                        <RadioButton x:Name="ColorBlack" 
                                     GroupName="BgColor"
                                     Checked="ColorRadio_Checked"
                                     Tag="#000000"
                                     ToolTip="أسود نقي"
                                     Margin="0,0,12,12">
                            <Border Width="50" Height="50" CornerRadius="25" Background="#000000" BorderBrush="#64748b" BorderThickness="2"/>
                        </RadioButton>
                    </WrapPanel>
                </StackPanel>
                
                <!-- Buttons -->
                <StackPanel Grid.Row="8" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right">
                    <Button x:Name="SaveButton" 
                            Padding="18,10" 
                            Style="{StaticResource GoldButtonStyle}"
                            Click="SaveButton_Click"
                            Margin="0,0,15,0">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Save24" Style="{StaticResource ActionIconStyle}"/>
                            <TextBlock Text="حفظ" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="CancelButton" 
                            Padding="18,10" 
                            Style="{StaticResource SecondaryButtonStyle}"
                            Click="CancelButton_Click">
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Dismiss24" Style="{StaticResource TopBarIconStyle}"/>
                            <TextBlock Text="إلغاء" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</Window>
